import React from 'react'

// Phase 0.5 stub — full implementation lands in Phase 3.
// Add / rename / remove / toggle routing mode (forward ↔ crm) on
// each email alias. First-class page under Settings.

export function EmailAliasesPage(): React.ReactElement | null {
  return null
}
