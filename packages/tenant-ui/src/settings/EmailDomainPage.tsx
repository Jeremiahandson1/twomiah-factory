import React from 'react'

// Phase 0.5 stub — full implementation lands in Phase 3.
// BYOD walkthrough: shows DNS records (SPF / DKIM / DMARC / MX /
// CNAMEs for app + website) and a Verify button that polls SendGrid
// domain-auth status via the tenant backend.

export function EmailDomainPage(): React.ReactElement | null {
  return null
}
