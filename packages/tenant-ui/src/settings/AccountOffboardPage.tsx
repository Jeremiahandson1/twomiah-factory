import React from 'react'

// Phase 0.5 stub — full implementation lands in Phase 4.
// One-click Leave Twomiah: explains the 30-day grace period,
// queues data export, triggers domain transfer-out (EPP code),
// cancels Stripe subscription at period end.

export function AccountOffboardPage(): React.ReactElement | null {
  return null
}
