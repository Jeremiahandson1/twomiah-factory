{
  "name": "twomiah-factory",
  "private": true,
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "mobile:dev": "cd apps/mobile && expo start",
    "mobile:android": "cd apps/mobile && expo run:android",
    "mobile:ios": "cd apps/mobile && expo run:ios",
    "mobile:build:apk": "cd apps/mobile && eas build --platform android --profile preview",
    "mobile:build:ios": "cd apps/mobile && eas build --platform ios --profile preview",
    "check:features": "bun run scripts/check-feature-vocabulary.ts"
  },
  "devDependencies": {
    "turbo": "^2.10.12"
  },
  "//overrides": "Security: force patched versions of transitive production deps flagged by `bun audit --prod` (ws via supabase-realtime; fast-xml-parser/builder via aws-sdk-s3).",
  "overrides": {
    "ws": "^8.21.3",
    "fast-xml-parser": "^5.11.1",
    "fast-xml-builder": "^1.3.1",
    "sharp": "^0.35.4"
  }
}
