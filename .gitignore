# dependencies (bun install)
node_modules
templates/**/package-lock.json

# output
out
dist
.turbo
*.tgz
*.zip

# generated builds
generated/

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Claude Code
.claude/

# Temp/debug files
tmp_*

# Template lock files (generated on install)
templates/**/bun.lock

# Standalone-app lock files (generated on install — workspace uses root bun.lock)
apps/roof-estimator/**/bun.lock

# Local-render proof-of-concept artifacts (regeneratable via render scripts)
templates/website-premium-*/composition-*.html
templates/website-premium-*/index.html
templates/website-premium-*/about.html
templates/website-premium-*/services.html
templates/website-premium-*/contact.html
templates/website-premium-*/data/samples/composition-ai.json
apps/api/preview-*.html

# Windows artifacts
nul/

# Mobile app secrets — never commit
apps/mobile/google-services.json
apps/mobile/google-services.*.json
apps/mobile/google-play-service-account.json
apps/mobile/AuthKey_*.p8

# Test harness outputs — audit JSON per run; can be regenerated
apps/api/scripts/test-audit/

# Test-tenant tracking + bundled SQL — both regenerable, never commit
apps/api/.path-aplusplus-test-tenants.json
twomiah-migrations.sql
