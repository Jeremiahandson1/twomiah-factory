// What this vertical does differently in the shared Reports page (see ./shared).
import type { ReportingConfig } from './shared'

export const REPORTING: ReportingConfig = { jobs: false, quotes: false, projects: false }
