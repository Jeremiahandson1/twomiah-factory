// What this vertical does differently in the shared invoices + quotes pages (see ../shared).
import type { InvoicingConfig } from './shared'

export const INVOICING: InvoicingConfig = { clientLabel: 'Customer', projects: false, jobs: true, quoteNamePlaceholder: 'e.g. Roof reseal and brake service' }
