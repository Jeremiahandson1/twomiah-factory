// What this vertical does differently in the shared invoices + quotes pages (see ../shared).
import type { InvoicingConfig } from './shared'

export const INVOICING: InvoicingConfig = { clientLabel: 'Client', projects: false, jobs: false, quoteNamePlaceholder: 'e.g. Webb anniversary dinner' }
