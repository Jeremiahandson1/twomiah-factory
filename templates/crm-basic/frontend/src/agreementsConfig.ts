// Behaviour flags for the shared Agreements page (packages/tenant-ui/src/agreements, vendored as ./shared).
import type { AgreementsConfig } from './shared'
export const agreementsConfig: AgreementsConfig = { recurrence: true }
