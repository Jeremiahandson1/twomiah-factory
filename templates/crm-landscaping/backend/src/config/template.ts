// Which Factory template this CRM was generated from. The feature registry (shared/featureRegistry.ts)
// is keyed by this id: it decides which features this tenant can switch on, what a plan tier seeds and
// what PUT /api/company/features accepts. scripts/check-feature-vocabulary.ts asserts it matches the
// template directory name.
export const CRM_TEMPLATE = 'crm-landscaping'
