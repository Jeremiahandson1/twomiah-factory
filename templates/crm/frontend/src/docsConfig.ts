// What this vertical does differently in the shared Documents page (see ./shared).
import type { DocumentsConfig } from './shared'

export const DOCUMENTS: DocumentsConfig = { projects: true, markup: true, types: ['general', 'contract', 'permit', 'drawing', 'photo', 'invoice', 'receipt', 'other'] }
