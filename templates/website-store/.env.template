# ----------------------------------------------------------------------------
# {{COMPANY_NAME}} storefront — environment
# ----------------------------------------------------------------------------
# The storefront is DB-free. All catalog reads, checkout, and order lookups go
# through the crm-store backend PUBLIC API.

# Base URL of the crm-store backend (server-only — never exposed to the client).
CRM_STORE_API_URL="{{BACKEND_URL}}"

# Public base URL of THIS storefront (canonical links, sitemap, OG images).
BASE_URL="{{SITE_URL}}"

# Google Analytics 4 measurement ID (G-XXXXXXXXXX). Leave empty to skip gtag.
NEXT_PUBLIC_GA_ID=""
